# Tested with Python 3.12

# Configuration
python-dotenv==1.0.1

# Web scraping
playwright==1.48.0
beautifulsoup4==4.12.3
lxml==5.3.0

# Embeddings (local, no API cost) + reranker (cross-encoder, local)
sentence-transformers==3.3.1

# Vector database (self-hosted service)
qdrant-client==1.12.1

# In-process index used only as an independent retrieval cross-check vs. Qdrant
faiss-cpu==1.9.0

# LLM providers (swappable via LLM_PROVIDER / the LLM factory)
anthropic==0.39.0
openai==1.54.4
# Pin httpx: 0.28 removed the `proxies` arg that openai 1.54 still passes.
httpx==0.27.2

# API server + streaming
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.2

# Tables in the analytics CLI
tabulate==0.9.0
