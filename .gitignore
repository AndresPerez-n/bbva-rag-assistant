# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
build/
dist/

# Virtual environments
venv/
.venv/
env/

# Environment / secrets
.env
.env.local

# Scraped data and runtime artifacts (regenerated by the pipeline).
# Keep the directories (via .gitkeep) but ignore their generated contents.
data/raw/*
data/clean/*
!data/raw/.gitkeep
!data/clean/.gitkeep
data/history.db
*.db

# Model / HF caches
.cache/
models/

# IDE / OS
.vscode/
.idea/
.DS_Store
